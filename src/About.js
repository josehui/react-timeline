import { Typography } from "@material-ui/core"
import { Box } from "@material-ui/core";


export default () => {
    return (
        <Box mx={4}>
        <Typography>Contact us at xxx</Typography>
        </Box>        
    );
}